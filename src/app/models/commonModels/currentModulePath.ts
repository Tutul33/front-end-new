export interface currentModulePath{
    moduleId:number,
    modulePath:string,
    menuId:number,
    menuPath: string,
    canCreate: boolean,
    canView: boolean,
    canEdit: boolean,
    canDelete: boolean
}